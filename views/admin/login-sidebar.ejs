<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Login - JediCare Care Medical centre</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #f6fbf7 0%, #ffffff 100%);
      color: #0d1b12;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    .login-container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
      border: 1px solid #e5eee7;
      width: 100%;
      max-width: 400px;
      padding: 40px;
      margin: 20px;
    }
    
    .login-header {
      text-align: center;
      margin-bottom: 32px;
    }
    
    .login-logo {
      width: 60px;
      height: 60px;
      border-radius: 15px;
      background: linear-gradient(135deg, #23b371, #a7f3c6);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 800;
      color: #06381f;
      font-size: 24px;
      margin: 0 auto 16px;
      border: 2px solid #d6f5e4;
    }
    
    .login-title {
      font-size: 24px;
      font-weight: 800;
      color: #0d1b12;
      margin-bottom: 8px;
    }
    
    .login-subtitle {
      color: #4d6b57;
      font-size: 14px;
    }
    
    .form-group {
      margin-bottom: 20px;
    }
    
    .form-group label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
      color: #0d1b12;
      font-size: 14px;
    }
    
    .form-control {
      width: 100%;
      padding: 12px 16px;
      border: 1px solid #e5eee7;
      border-radius: 10px;
      font-size: 14px;
      font-family: inherit;
      background: #ffffff;
      transition: all 0.2s ease;
    }
    
    .form-control:focus {
      outline: none;
      border-color: #23b371;
      box-shadow: 0 0 0 3px rgba(35, 179, 113, 0.1);
    }
    
    .btn {
      width: 100%;
      padding: 12px 20px;
      border: none;
      border-radius: 10px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
      display: inline-block;
      text-align: center;
    }
    
    .btn-primary {
      background: linear-gradient(135deg, #23b371, #a7f3c6);
      color: #06381f;
      border: 1px solid #7ee2ad;
    }
    
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(35, 179, 113, 0.3);
    }
    
    .btn-secondary {
      background: transparent;
      color: #23b371;
      border: 1px solid #23b371;
      margin-top: 12px;
    }
    
    .btn-secondary:hover {
      background: #f6fbf7;
    }
    
    .alert {
      padding: 12px 16px;
      border-radius: 8px;
      margin-bottom: 20px;
      font-size: 14px;
    }
    
    .alert-error {
      background: #fef2f2;
      border: 1px solid #fecaca;
      color: #dc2626;
    }
    
    .alert-success {
      background: #f0fdf4;
      border: 1px solid #bbf7d0;
      color: #16a34a;
    }
    
    .back-link {
      text-align: center;
      margin-top: 24px;
      padding-top: 24px;
      border-top: 1px solid #e5eee7;
    }
    
    .back-link a {
      color: #4d6b57;
      text-decoration: none;
      font-size: 14px;
    }
    
    .back-link a:hover {
      color: #23b371;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-header">
      <div class="login-logo">JM</div>
      <h1 class="login-title">Admin Login</h1>
      <p class="login-subtitle">JediCare Care Medical centre</p>
    </div>
    
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert-error">
        Invalid password. Please try again.
      </div>
    <% } %>
    
    <% if (typeof success !== 'undefined' && success) { %>
      <div class="alert alert-success">
        <%= success %>
      </div>
    <% } %>
    
    <form method="post" action="/admin/login">
      <div class="form-group">
        <label for="password">Admin Password</label>
        <input type="password" id="password" name="password" class="form-control" 
               placeholder="Enter admin password" required>
      </div>
      
      <button type="submit" class="btn btn-primary">
        🔐 Login to Admin
      </button>
    </form>
    
    <div class="back-link">
      <a href="/">← Back to Website</a>
    </div>
  </div>
</body>
</html>
