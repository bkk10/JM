<div class="admin-card">
  <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px;">
    <h1><%= title %></h1>
    <a href="/admin/blog" class="btn btn-secondary">← Back to Blog</a>
  </div>
  
  <form method="post" action="<%= action %>">
    <div class="form-group">
      <label>Blog Post Title *</label>
      <input type="text" name="title" class="form-control" 
             value="<%= post.title || '' %>" 
             placeholder="Enter blog post title" required>
    </div>
    
    <div class="form-group">
      <label>Author *</label>
      <input type="text" name="author" class="form-control" 
             value="<%= post.author || 'Admin' %>" 
             placeholder="Author name" required>
    </div>
    
    <div class="form-group">
      <label>Excerpt (Optional)</label>
      <textarea name="excerpt" class="form-control" rows="3"
                placeholder="Brief summary of the blog post..."><%= post.excerpt || '' %></textarea>
      <small style="color: #6b7280; margin-top: 4px; display: block;">
        If left empty, the first 150 characters of the content will be used.
      </small>
    </div>
    
    <div class="form-group">
      <label>Status</label>
      <select name="status" class="form-control">
        <option value="published" <%= (post.status === 'published' ? 'selected' : '') %>>Published</option>
        <option value="draft" <%= (post.status === 'draft' ? 'selected' : '') %>>Draft</option>
      </select>
    </div>
    
    <div class="form-group">
      <label>Content *</label>
      <textarea name="content" class="form-control" rows="15"
                placeholder="Write your blog post content here..."
                required><%= post.content || '' %></textarea>
      <small style="color: #6b7280; margin-top: 4px; display: block;">
        You can use line breaks to separate paragraphs. They will be automatically formatted.
      </small>
    </div>
    
    <div style="display: flex; gap: 12px; align-items: center; padding-top: 20px; border-top: 1px solid #e5eee7;">
      <button type="submit" class="btn btn-primary">
        <%= post.id ? 'Update Post' : 'Publish Post' %>
      </button>
      <a href="/admin/blog" class="btn btn-secondary">Cancel</a>
    </div>
  </form>
</div>

<style>
.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  margin-bottom: 6px;
  font-weight: 600;
  color: #0d1b12;
  font-size: 14px;
}

.form-control {
  width: 100%;
  padding: 10px 14px;
  border: 1px solid #e5eee7;
  border-radius: 8px;
  font-size: 14px;
  font-family: inherit;
  background: #ffffff;
  transition: all 0.2s ease;
}

.form-control:focus {
  outline: none;
  border-color: #23b371;
  box-shadow: 0 0 0 3px rgba(35, 179, 113, 0.1);
}

.form-control::placeholder {
  color: #9ca3af;
}

small {
  font-size: 12px;
  line-height: 1.4;
}
</style>
